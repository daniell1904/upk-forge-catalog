services:
  autobrr:
    image: ghcr.io/autobrr/autobrr:v1.86.0
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 7474:7474
    volumes:
      - ${VOL_1}:/config
    environment:
      - TZ=$TZ
