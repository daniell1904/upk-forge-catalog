services:
  autobrr:
    image: ghcr.io/autobrr/autobrr:v1.79.0
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 7474:7474
    volumes:
      - ./config:/config
    environment:
      - TZ=$TZ
