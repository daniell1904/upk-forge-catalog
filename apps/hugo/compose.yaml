services:
  hugo:
    image: ghcr.io/gohugoio/hugo:v0.166.0
    restart: unless-stopped
    command:
      - server
      - --bind=0.0.0.0
    ports:
      - 1313:1313
    volumes:
      - ${VOL_1}:/project
      - ${VOL_2}:/cache
