services:
  suggestarr:
    image: docker.io/ciuse99/suggestarr:v2.14.0
    ports:
      - "5007:5000"
    volumes:
      - ./config_files:/app/config/config_files
    environment:
      - TZ=${TZ}
      - LOG_LEVEL=info
      - SUGGESTARR_PORT=5000
    restart: unless-stopped
