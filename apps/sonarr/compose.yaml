services:
  sonarr:
    image: linuxserver/sonarr:4.0.19
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8989:8989
    volumes:
      - ${VOL_1}:/config
      - ${VOL_2}:/tv
      - ${VOL_3}:/downloads
    environment:
      - PGID=1000
      - PUID=1000
      - TZ=$TZ
