services:
  firefly:
    image: uusec/firefly:4.4
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./firefly-lib-modules:/lib/modules
      - ./conf:/firefly/conf
    environment:
      - FIREFLY_PASSWORD=${FIREFLY_PASSWORD}
