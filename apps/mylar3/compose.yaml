services:
  mylar3:
    image: linuxserver/mylar3:0.9.0
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8090:8090
    volumes:
      - ${VOL_1}:/config
      - ${VOL_2}:/comics
      - ${VOL_3}:/downloads
    environment:
      - PGID=1000
      - PUID=1000
      - TZ=$TZ
