services:
  sakurafrp:
    image: docker.io/natfrp/launcher:3.1.8
    network_mode: host
    volumes:
      - ./data:/run
    environment:
      - TZ=${TZ}
      - LANG=zh_CN.UTF-8
      - NATFRP_TOKEN=${wizard_natfrp_token}
      - NATFRP_REMOTE=${wizard_natfrp_remote}
    restart: unless-stopped
