services:
  zigbee2mqtt:
    image: koenkk/zigbee2mqtt:2.14.1
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ${VOL_1}:/app/data
      - /run/udev:/run/udev
    environment:
      - TZ=UTC
