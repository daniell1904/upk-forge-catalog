services:
  calibre-web:
    image: docker.io/linuxserver/calibre-web:0.6.27
    ports:
      - "8083:8083"
    volumes:
      - ./data:/config
      - ./books:/books
    environment:
      - TZ=${TZ}
      - PUID=1000
      - PGID=1000
    restart: unless-stopped
