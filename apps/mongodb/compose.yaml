services:
  mongo:
    image: mongo:8.2.2
    restart: unless-stopped
    ports:
      - 27017:27017
    volumes:
      - ./data/configdb:/data/configdb
      - ./data/db:/data/db
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=$TZ
