services:
  pinchflat:
    image: ghcr.io/kieraneglin/pinchflat:v2025.6.6
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8945:8945
    volumes:
      - ${VOL_1}:/config
      - ${VOL_2}:/downloads
    environment:
      - TZ=$TZ
      - PUID=1000
      - PGID=1000
