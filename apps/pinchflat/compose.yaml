services:
  pinchflat:
    image: ghcr.io/kieraneglin/pinchflat:v2025.6.6
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8945:8945
    volumes:
      - ./config:/config
      - ${VOL_1}:/downloads
    environment:
      - TZ=$TZ
      - PUID=1000
      - PGID=1000
