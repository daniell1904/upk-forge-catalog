services:
  cloudbeaver:
    image: dbeaver/cloudbeaver:26.0.5
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8978:8978
    volumes:
      - ./workspace:/opt/cloudbeaver/workspace
