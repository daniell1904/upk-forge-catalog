services:
  cloudbeaver:
    image: dbeaver/cloudbeaver:26.2.0
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8978:8978
    volumes:
      - ${VOL_1}:/opt/cloudbeaver/workspace
