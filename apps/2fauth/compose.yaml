services:
  2fauth:
    image: 2fauth/2fauth:6.1.3
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8000:8000
    volumes:
      - ./2fauth-2fauth:/2fauth
    environment:
      - APP_KEY=${APP_KEY}
