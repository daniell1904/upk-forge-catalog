services:
  2fauth:
    image: 2fauth/2fauth:8.0.2
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8000:8000
    volumes:
      - ${VOL_1}:/2fauth
    environment:
      - APP_KEY=${APP_KEY}
