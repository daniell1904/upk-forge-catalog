services:
  portainer:
    image: portainer/portainer-ce:2.45.0
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8000:8000
      - 9000:9000
      - 9443:9443
    volumes:
      - ${VOL_1}:/data
      - ${VOL_2}:/var/run/docker.sock
