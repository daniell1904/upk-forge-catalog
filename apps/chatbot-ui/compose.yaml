services:
  chatbot-ui:
    image: ghcr.io/mckaywrigley/chatbot-ui:main
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 3080:3000
    environment:
      - PGID=1000
      - PUID=1000
      - TZ=$TZ
