services:
  note-mark-aio:
    image: ghcr.io/enchant97/note-mark-aio:0.19.5
    restart: unless-stopped
    ports:
      - 8000:8000
    volumes:
      - ${VOL_1}:/data
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGINS="http://example.com:8000"
