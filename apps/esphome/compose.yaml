services:
  esphome:
    image: ghcr.io/esphome/esphome:2026.8.2
    restart: unless-stopped
    network_mode: host
    ports:
      - 6052:6052
    volumes:
      - ${VOL_1}:/config
    environment:
      - ESPHOME_DASHBOARD_USE_PING=false
      - TZ=$TZ
