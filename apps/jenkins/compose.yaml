services:
  jenkins:
    image: jenkins/jenkins:lts-jdk17
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8080:8080
    volumes:
      - ./data:/data
      - ./jenkins-var-jenkins-home:/var/jenkins_home
