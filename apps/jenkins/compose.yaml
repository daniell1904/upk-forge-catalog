services:
  jenkins:
    image: jenkins/jenkins:lts-jdk17
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8080:8080
    volumes:
      - ${VOL_1}:/data
      - ${VOL_2}:/var/jenkins_home
