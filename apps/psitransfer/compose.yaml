services:
  psitransfer:
    image: psitrax/psitransfer:v2.4.4
    restart: unless-stopped
    ports:
      - 13001:3000
    volumes:
      - ${VOL_1}:/data
    environment:
      - PSITRANSFER_ADMIN_PASS=${PSITRANSFER_ADMIN_PASS}
