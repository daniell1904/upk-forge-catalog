services:
  stremio:
    image: tsaridas/stremio-docker:v1.3.4
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 11470:11470
      - 8100:8080
    volumes:
      - ./config:/root/.stremio-server
    environment:
      - NO_CORS=1
      - AUTO_SERVER_URL=1
