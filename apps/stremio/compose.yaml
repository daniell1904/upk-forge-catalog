services:
  stremio:
    image: tsaridas/stremio-docker:v1.3.10
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 11470:11470
      - 8100:8080
    volumes:
      - ${VOL_1}:/root/.stremio-server
    environment:
      - NO_CORS=1
      - AUTO_SERVER_URL=1
