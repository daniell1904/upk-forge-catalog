services:
  kavita:
    image: docker.io/jvmilazz0/kavita:0.9.1
    ports:
      - "5000:5000"
    volumes:
      - ./config:/kavita/config
      - ${LIBRARY_DIR}:/library
    environment:
      - TZ=${TZ}
    restart: unless-stopped
