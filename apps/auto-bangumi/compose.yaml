services:
  auto-bangumi:
    image: ghcr.io/estrellaxd/auto_bangumi:3.3.5
    ports:
      - "7892:7892"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    environment:
      - TZ=${TZ}
      - PUID=1000
      - PGID=1000
      - UMASK=022
    restart: unless-stopped
