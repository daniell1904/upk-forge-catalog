id: dbgate
name: DBGate
author: Community
category: Developer
description: Web based UI for managing and exploring SQL database data
ugos:
  app_id: com.upkforge.docker.dbgate
  version: 0.1.0
  port: 3000
  open_type: tab
  tag_type: devtool
parameters:
- key: VOL_1
  label: Root .dbgate folder
  help: Folder on the NAS for DBGate's root .dbgate. It is mounted into the container at /root/.dbgate; DBGate has full read and write access to it.
  label_de: Root .dbgate-Ordner
  help_de: Ordner auf der NAS für DBGates Root .dbgate. Wird im Container unter /root/.dbgate eingebunden; DBGate hat vollen Lese- und Schreibzugriff darauf.
  type: path
volumes:
- container_path: /root/.dbgate
  label: Root .dbgate
  default_host_subpath: dbgate-root-dbgate
