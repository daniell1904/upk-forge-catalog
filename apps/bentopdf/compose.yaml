services:
  bentopdf:
    image: ghcr.io/alam00000/bentopdf-simple:2.8.8
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 3000:8080
    environment:
      - PUID=1000
      - PGID=1000
      - DISABLE_IPV6=false
