services:
  netalertx:
    image: jokobsk/netalertx:26.9.0
    restart: unless-stopped
    network_mode: host
    volumes:
      - ${VOL_1}:/app/config
      - ${VOL_2}:/app/db
    environment:
      - TZ=UTC
      - PORT=8080
      - ALWAYS_FRESH_INSTALL=false
