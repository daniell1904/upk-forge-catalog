id: nexterm
name: Nexterm
author: Community
category: Others
description: The open source server management software for SSH, VNC & RDP. This is in BETA.
ugos:
  app_id: com.upkforge.docker.nexterm
  version: 0.1.0
  port: 6989
  open_type: tab
  tag_type: utility
parameters:
- key: VOL_1
  label: App data folder
  help: Folder on the NAS for Nexterm's app data. It is mounted into the container at /app/data; Nexterm has full read and write access to it.
  type: path
volumes:
- container_path: /app/data
  label: App data
  default_host_subpath: nexterm-app-data
