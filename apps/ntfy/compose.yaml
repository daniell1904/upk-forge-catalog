services:
  app:
    image: binwiederhier/ntfy:v2.28.0
    restart: unless-stopped
    command:
      - serve
    ports:
      - 7200:80
    volumes:
      - ${VOL_1}:/var/cache/ntfy
      - ${VOL_2}:/etc/ntfy
    environment:
      - TZ=UTC
