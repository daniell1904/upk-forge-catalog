services:
  logseq:
    image: correctroad/logseq:v0.10.15
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 3325:3000
      - 12315:12315
    volumes:
      - ${VOL_1}:/config
      - ${VOL_2}:/notes
    environment:
      - PGID=0
      - PUID=0
