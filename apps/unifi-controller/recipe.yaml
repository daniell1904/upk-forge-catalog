id: unifi-controller
name: Unifi-controller [legacy]
author: Team
category: Networking
description: For Unifi to adopt other devices, e.g. an Access Point, it is required to change the inform IP address. Because Unifi runs inside Docker by default it uses an IP address not accessible by other devices. To change this go to Settings > System Settings > Controller Configuration and set the Controller Hostname/IP to a hostname or IP address accessible by your devices. Additionally the checkbox "Override inform host with controller hostname/IP" has to be checked, so that devices can connect to the controller during adoption (devices use the inform-endpoint during adoption).
homepage: https://ui.com
source_code: https://github.com/linuxserver/docker-unifi-controller
ugos:
  app_id: com.upkforge.docker.unificontroller
  version: 0.1.0
  port: 3478
  open_type: tab
  tag_type: utility
parameters:
- key: VOL_1
  label: Config folder
  help: Folder on the NAS for Unifi-controller [legacy]'s config. It is mounted into the container at /config; Unifi-controller [legacy] has full read and write access to it.
  label_de: Konfiguration-Ordner
  help_de: Ordner auf der NAS für Unifi-controller [legacy]s Konfiguration. Wird im Container unter /config eingebunden; Unifi-controller [legacy] hat vollen Lese- und Schreibzugriff darauf.
  type: path
volumes:
- container_path: /config
  label: Config
  default_host_subpath: unifi-controller-config
